function carol(den,res){
  var den=prompt("just work please");
  var res=den.charAt(0);
  var res2=den.charAt(den.length-1);
  var final=res.concat(res2).toUpperCase();
console.log(res);
console.log(res2);
console.log(final);

}

carol();
